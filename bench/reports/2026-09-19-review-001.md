# Doppel Agent code-review acceptance — 2026-09-19

**Boundary:** One synthetic 45-line Python file with three seeded defects and
one safe control. The only source file available to the agent was `service.py`.
`DEEPSEEK_API_KEY` was read from a user-authorized external `.env` only in
process memory. The credential and its path are not stored in the repository
or report. No MedOps source code was transmitted.

| Trial | Prompt | Model | Status | Tool evidence | Model-reported tokens |
|---|---|---|---|---|---:|
| 1 | Guided security areas | `deepseek-flash` | completed | `read_file(service.py)`; no write/command | 7,687 |
| 2 | Blind generic review | `deepseek-flash` | completed | `read_file(service.py)`, `list_files(.)`; no write/command | 6,055 |

Manual adjudication of the **blind** trial against `answer_key.json`:

- **3/3 seeded defects identified** with the relevant source lines and plausible
  impact/fix: missing owner filter in `fetch_note` (`service.py:16`), SQL
  injection in `search_notes` (`service.py:26`), and path traversal in
  `save_attachment` (`service.py:32–34`). Unit tests reproduce all three.
- The safe control `update_note_title` was **not** misidentified as SQL
  injection or a missing owner check.
- Two additional observations were reported: repeated `create_schema` calls
  fail, and `update_note_title` relies on the caller to commit. Both depend on
  an unstated lifecycle/transaction contract. They are **unconfirmed**, not
  counted as seeded security findings.
- The guided trial also found all three seeded defects, but additionally
  reported a speculative schema-hardening issue.

**Conclusion:** This demonstrates one real model/tool review path and correct
identification of the three planted defects. It does **not** establish a
general review success rate, production-code performance, a cost comparison,
or the reference project's published metrics. Raw run reports remain local in
ignored `.bench-results/` so the reviewer can inspect full answers and usage.
